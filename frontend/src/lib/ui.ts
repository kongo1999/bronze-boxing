// Shared UI class strings — single source of truth so inputs look identical
// across every form and don't drift. Focus ring is bronze (the secondary tone).
export const inputCls =
  "w-full rounded-xl border border-line bg-elevated px-3 py-2.5 text-sm outline-none " +
  "transition-colors placeholder:text-faint " +
  "focus:border-bronze focus:ring-2 focus:ring-bronze/25";
