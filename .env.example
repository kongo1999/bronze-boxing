# MongoDB connection string.
# Local native install:  mongodb://127.0.0.1:27017/bronze-boxing
# Atlas (cloud):         mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/bronze-boxing
MONGODB_URI="mongodb://127.0.0.1:27017/bronze-boxing"

# Currency symbol shown in the ledger UI (cash-only for now).
NEXT_PUBLIC_CURRENCY="$"
